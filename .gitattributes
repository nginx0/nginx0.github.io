# Set default behavior to normalize line endings
* text=auto

# Force LF for shell scripts
*.sh text eol=lf

# Force CRLF for Windows batch files
*.{cmd,bat} text eol=crlf

# Mark images as binary
*.{png,jpg,ico} binary

# --- FIX FOR LANGUAGE STATS ---
# Ignore the large HTA file and everything in the images folder
images/** linguist-vendored

# Ignore all HTML and CSS files from the language bar
*.html linguist-detectable=false
*.css linguist-detectable=false

# Specifically target the HTA extension
*.hta linguist-detectable=false